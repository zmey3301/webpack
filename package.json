{
  "name": "soulstring-vue-boilerplate",
  "version": "1.3.1",
  "license": "MIT",
  "description": "Vue template with adaptive, fetch-based http and ssr",
  "scripts": {
    "docs": "cd docs && gitbook serve",
    "docs:deploy": "bash ./deploy-docs.sh"
  },
  "devDependencies": {
    "vue-cli": "^2.8.1"
  },
  "dependencies": {
    "gitbook": "^3.2.3",
    "gitbook-cli": "^2.3.2"
  }
}
